# AGENTS.md

## 项目说明

本项目是“数据共情者”比赛方案：面向人工客服工作台右侧的客服辅助 Copilot，融合当前可见的聊天、订单、订单物流字段、物流工单和售后工单，输出带证据的事实、意图、可观察情绪信号、风险判断、回复草稿和下一步处理建议。

产品不是面向消费者的自动客服机器人。发送回复、升级、创建或更新工单、退款、赔偿、线下打款等动作必须由人工确认。

## 文档事实来源

- `方案文档/01-方案规格-SPEC.md` 是产品、数据、Agent、风险边界和评测口径的唯一事实来源。
- `方案文档/02-实施计划-PLAN.md` 描述实现顺序和交付门槛。
- `方案文档/03-方案冻结版-详细.md` 是面向评审和团队阅读的长文档，不能自行引入与 SPEC 不一致的规则。
- `方案文档/04-评测协议-详细.md` 规定数据划分、评测样本、对照组、指标和错误类型。
- `方案文档/05-三分钟Demo脚本-详细.md` 规定演示叙事，演示能力必须能在 SPEC 和实际运行结果中找到依据。
- `方案文档/06-方案审查报告.md` 是审查记录和修复依据，不是产品规则来源。

修改产品规则、时间过滤、风险定级、评测口径或 Provider 契约时，先修改 SPEC，再同步其他文档并更新版本记录。

## 数据与时间快照

所有分析必须携带 `as_of_time`，不得使用未来信息：

```text
chat.message_time <= as_of_time
order.下单时间 <= as_of_time
付款时间、发货时间、订单状态分别按字段断言 <= as_of_time
workorder.create_time <= as_of_time
只有 workorder.complete_time <= as_of_time 时才显示完结状态和完成时间
```

聊天表中事后回填的关联订单号、关联工单号只能作为潜在关联键，不能绕过时间过滤。订单号、工单号、会话 ID、买家标识等 ID 必须按字符串读取，禁止被解析为浮点数。

S00015 消息 4 的客服表述与工单创建时间存在真实冲突。系统应保留两类来源并标记 `source_inconsistency`，不得让模型自行选择一方。

## Agent 和 Provider 约束

- 使用一个主 Agent 加结构化工具，不为了展示而增加多 Agent。
- 程序负责数据权限、时间过滤、规则风险和人工确认；模型负责抽取、归纳、解释和生成草稿。
- 工具只能读取数据或生成草稿，不能直接执行外部业务动作。
- Provider 至少保持 `QwenProvider`、`MockProvider`、`RuleProvider` 的统一 JSON 契约。
- Qwen 不可用时必须可降级到本地 Mock/Rule 流程，并在界面显示当前 Provider 和降级原因。
- 模型不能降低规则已经确定的高风险等级。

## 风险和情绪口径

情绪只表示可观察的服务信号：

```text
negative_signal: none | present | unclear
escalation: no | yes | unclear
```

不做心理诊断，不把“医生/产检医生”等词直接等同于就医事件。

风险等级定义：

- `P0`：人身安全、就医或监管升级，须即时人工介入。
- `P1`：资金或风控相关，须当日核实处理。
- `P2`：体验类负面情绪，按标准流程处理。
- `待定级`：证据不足，使用 `needs_verification`，不能直接升级为 P0/P1。

## 评测约定

评测使用四组对照：

- `Chat-only`
- `Snapshot`
- `Full Agent`
- `Rules-only`

默认评测样本只取 `is_target_buyer_message=1` 的买家消息时点；客服消息时点仅用于时间泄漏回归测试。

冻结测试集固定为 12 个会话，冻结后不得据此调参。PPT 和视频中的效果数字只能来自冻结测试集的真实运行结果，不得来自 Mock 固定答案或手工填写。

`unclear` 必须同时报告覆盖率和覆盖内严格准确率，不能用全量输出 `unclear` 获得虚假的安全表现。评测报告应保留样本 ID、Provider、模型版本、Prompt 版本、规则/政策版本、原始 JSON、工具调用、Token、耗时、标签、错误类型和证据引用。

## 文档与文件命名

方案文档使用数字前缀表示阅读顺序：

```text
01-方案规格-SPEC.md
02-实施计划-PLAN.md
03-方案冻结版-详细.md
04-评测协议-详细.md
05-三分钟Demo脚本-详细.md
06-方案审查报告.md
```

新增方案文档应放入 `方案文档/`，使用中文说明性名称，并带有数字前缀。不要创建同义重复版本；若需要新版本，在文档内增加变更记录并说明替代关系。

## 开发和验证要求

修改前先读取相关文档和实际数据结构；只做完成当前任务所需的最小修改。

修改后至少完成与变更对应的检查：

- Markdown 文件可正常读取，标题和代码块未损坏。
- SPEC、冻结版、评测协议、Demo 脚本的时间过滤和评测组口径一致。
- 时间快照相关修改至少检查未来工单、完成状态和 `source_inconsistency` 案例。
- 评测相关修改至少检查冻结集数量、四组对照和 `unclear` 计分定义。
- 数据脚本读取 ID 时必须显式使用字符串类型，并检查关联命中率。

不要在没有明确授权的情况下执行提交、推送、发布、删除数据或连接真实业务系统。
