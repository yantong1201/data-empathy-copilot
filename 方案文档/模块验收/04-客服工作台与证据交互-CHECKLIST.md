# 工作台与证据交互检查项

- 版本：v0.3
- 日期：2026-09-13

| ID | 关联任务 | 检查与通过标准 | 验证方式/证据 | 状态 |
|---|---|---|---|---|
| UI-C001 | UI-T001 | 按 [交互基线](../模块规格/04-客服工作台与证据交互-SPEC.md#interaction) 接入真实快照，消息回放和三个快捷入口重算时点；缺时间禁用；搜索进入不泄漏未来字段 | 操作路径与对应快照 JSON：消息→创建→完成→回到首条；S00024/S00001 完成入口禁用；创建前按潜在工单号搜索→进入会话仍无可见工单 | 待验证 |
| UI-C002 | UI-T002 | 证据/草稿语义正确；工具抽屉请求、响应、来源、`as_of_time` 与 `analysis_id` 对应同次运行；查证不改变时点/草稿；已知编号不重复询问；人工升级为本地草稿且不调用第 9 个工具/真实接口 | 编辑草稿→查看聊天依据→打开/关闭工具抽屉→回到处理建议的录屏与运行记录；核对无实测记录时标待接入、过期运行不被展示为当前 | 待验证 |
| UI-C003 | UI-T003 | 确认校验当前 `analysis_id`、`as_of_time`、草稿 `revision`/`draft_hash` 和幂等 `operation_id`；过期结果拒绝确认；按允许转移写日志，安全失败不得确认 | 选择→异步替换结果→确认应拒绝→刷新当前分析→确认/取消→查看处置日志；核对业务快照和规则风险未改写 | 待验证 |
| UI-C004 | UI-T004 | 按 [验收边界](../模块规格/04-客服工作台与证据交互-SPEC.md#acceptance) 完成关键文字、键盘、窄屏回归；首屏可判断下一步并填入草稿，完整工具 JSON/校验明细/日志仅按需展开 | 桌面/窄屏截图及键盘操作录屏，覆盖快捷入口、抽屉关闭、状态确认/取消；核对聊天无遮挡、主要处理路径无需先打开详情 | 待验证 |

## 变更记录

- v0.1（2026-09-13）：补齐 HTML 快照、工具抽屉、紧凑状态入口的具体验收路径及效率原则；区分原型行为证据和真实接入证据，检查 ID 与待验证状态保持不变。
- v0.2（2026-09-13）：补充分析、安全校验、处置日志关联和允许转移的验收证据。
