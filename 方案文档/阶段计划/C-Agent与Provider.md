# 阶段 C：Agent 与三种 Provider

进入条件：A 的快照接口和 B 的正式 Schema 可用。涉及 AGENT-T001–T004；Mock/Rule 可先于 Qwen 并行。
退出条件：AGENT-C001–C004 全部通过；断网无密钥主流程可运行；统一 `analysis_id`/`run_id`、完整版本指纹、运行预算、停止/降级记录和 `safety_check_result` 可被工作台与评测读取。当前：待 B。
