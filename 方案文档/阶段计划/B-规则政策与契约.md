# 阶段 B：规则、政策与结构化契约

进入条件：阶段 A 退出。涉及 RISK-T001–T004；规则矩阵、政策清单和正式 Schema 可并行，但必须共用统一风险枚举、时区和来源引用约定。
退出条件：RISK-C001–C004 全部通过；缺证据不升级；统一 JSON、`why/why_not`、顶层 `source_inconsistency`、五项独立 `safety_check_result` 和非法输出降级路径可被 Agent 与 UI 共用。当前：待 A。
