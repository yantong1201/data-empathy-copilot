# 文档迁移与复核记录

- 版本：v0.2
- 日期：2026-09-13
- 用途：记录本次文档重组的章节映射、已采纳的审查修复和证据边界。
- 规则来源：本文件是迁移记录，不是产品规则来源。

## 章节迁移映射

| 旧正文主题 | 当前权威位置 | 说明 |
|---|---|---|
| 旧 SPEC 产品定义、赛题对齐、核心流程 | 01-方案规格-SPEC.md | 总 SPEC 保留全局摘要 |
| 旧 SPEC 数据/关联/快照 | 模块规格/01-数据与时间快照-SPEC.md | 含字段、唯一性、自检、三例夹具 |
| 旧 SPEC Agent/工具/Provider/成本 | 模块规格/03-Agent与Provider-SPEC.md | 函数签名和缓存键不改 |
| 旧 SPEC 情绪/风险/政策/安全/契约 | 模块规格/02-风险政策与结构化契约-SPEC.md | 含案例预期和回复安全 |
| 旧 SPEC UI/静态原型边界 | 模块规格/04-客服工作台与证据交互-SPEC.md | 静态基线和真实接入分开 |
| 旧 SPEC 评测 | 04-评测协议-详细.md；交付接口见模块 05 | 公式、名单和集合只维护于协议 |
| 旧 SPEC 工程目录/运行约定/扩展 | 模块规格/06-演示运行与提交-SPEC.md | 目标工程不等于当前实现 |
| 旧 PLAN A–F 顺序和门槛 | 02-实施计划-PLAN.md；阶段门槛在 阶段计划/ | 任务和检查项链接追溯 |
| 旧 PLAN 协作、Git、风险应对 | 02-实施计划-PLAN.md；执行任务在模块任务和阶段计划 | 仍是后续工程约定 |
| 旧冻结版、Demo、审查报告 | 原文件保留 | 分别作为叙事脚本和历史记录，不是新规则来源 |

原文件路径仍保留并可追溯；本次是在同一文件中将总 SPEC/PLAN 收敛为入口，并没有创建第二份旧正文，也没有删除原始 Excel、原型或审计脚本。

## 当前任务与状态修复

- 现有 24 个任务 ID、24 个检查 ID 保持不变。
- DATA-T001 负责工程目录、loader、字段映射和自检；DATA-T002–T004 负责快照、冲突夹具和主案例基线。
- RISK-T001/T002 负责规则、反例和情绪历史口径；RISK-T004 已固定 `risk.why/why_not`、顶层 `source_inconsistency` 和独立 `safety_check_result` 的承载及语义，不新增 Provider 顶层安全字段。
- 静态 UI 已有代码证据与真实接入/浏览器回归分开；任务仍待实现，检查项在无候选实现时为未开始。
- UI-T003 的队列状态触发规则已在工作台 SPEC 固化：人工确认驱动允许转移，`analysis_id` 关联处置日志；实现和证据仍由阶段 D 负责。
- EVAL-T001 的数据划分准备可先做；正式标注与冻结运行须使用已锁定规则和候选版本。冻结 12 和当前登记回归案例见评测协议。

## 协作与版本约定

沿用两人垂直切片：S00015 高风险、S00024 待核实、S00001 普通售后/图片扩展、评测与展示；每日保留变更说明、可复现命令、已知问题和下一步接续点。先定 JSON 契约再并行前后端，另一人用 Mock 路径复核切片。

目标 Git 标签：baseline-data、core-flow-ready、eval-ready、submission-candidate。每个标签对应 CHANGELOG 或发布说明，记录已完成能力、验证命令、限制和下一版风险。本次不执行 Git 标签、提交或发布。

## 风险应对登记

| 风险 | 触发 | 处理与证据 |
|---|---|---|
| Qwen 网络/密钥失败 | 超时/鉴权失败 | 自动 Mock/Rule，记录降级原因 |
| 工单时间泄漏 | 早期显示未来状态 | 集中走快照仓库并保留回归样本 |
| 负面语气误报 | 反例被升高 | 规则定级，模型不能改等级 |
| 证据不足过度断言 | S00024 等已有核实结论 | needs_verification、缺失字段和来源 |
| 图片缺失 | Excel 有路径无文件 | 主线不依赖视觉，扩展标合成 |
| 冻结集污染 | 结果反复变化 | 冻结 hash 和版本记录 |
| 展示脱节 | PPT 数字和候选运行不一致 | 冻结报告后再制作物料 |

## 审计辅助产物

方案文档/_audit_xlsx.py、方案文档/_audit_docx.py、方案文档/_media/ 是材料核对辅助产物，不是产品运行模块或规则来源。本次未删除或修改它们；命令输出应作为材料审计证据保存，不替代工程测试。

## 复核记录

- 2026-09-12：核对 Excel 表规模、字符串 ID 关联、三例时间和图片路径；未执行真实 loader、Provider 或评测。
- 2026-09-12：根据三模型反馈修复失效引用、规则重复、迁移缺口、状态语义和评测集合表述。
- 2026-09-13：补充正式 Provider Schema、风险规则矩阵、运行预算与 `analysis_id/run_id` 指纹、时区、工作台过期确认、`intent_ontology_v1` 和评测公式；上述正文分别以对应权威 SPEC/评测协议为准。

